import React from 'react';

const Header = () => (
	<div className="header">
		<div className="container">
			<h1 className="header__title">My Books Tracker</h1>
			<p className="header__subtitle">Keep track of the books you want to read and move them to readed when you've done it!</p>
		</div>
	</div>
	
);

export default Header;